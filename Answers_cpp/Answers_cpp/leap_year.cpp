#include<iostream>
using namespace std;
int main()
{
        int year;
        cout<<"Enter the year to check if leap year or not= ";
        cin>>year;
        if(year%4==0)
        {
             cout<<year<<" is a leap year";
        }
        else
            cout<<yea<<" is not a leap year";
        
        return 0;
}